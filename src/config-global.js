/**
 * Global configuration for the application
 */

export const CONFIG = {
  assetsDir: '',
  siteName: 'Sweden Relocators',
  serverUrl: process.env.NEXT_PUBLIC_API_URL || 'https://api.swedenrelocators.se',
};
